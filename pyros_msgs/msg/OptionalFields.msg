# The list of fields names that should be considered optional
string[] optional_field_names
# TODO: find a way to make that static, in the definition itself, and not dynamically...

# A boolean value per field, indicating if that field has been initialized or not
bool[] optional_field_initialized_